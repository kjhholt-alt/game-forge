# Claude calls go through claudex (Max-sub `claude -p` subprocess) — see claudex_anthropic_shim.py
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27
websockets>=12.0
Pillow>=10.0
aiofiles>=23.0
aiosqlite>=0.19
rich>=13.0
click>=8.0

# Dev dependencies
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
mypy>=1.10
