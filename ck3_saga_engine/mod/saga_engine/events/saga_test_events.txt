namespace = saga

##################################################
# Saga Engine — Phase 1 hello-world event
# saga.1 — proves the pipeline: skeleton -> tiger-clean -> game-load-clean
##################################################

saga.1 = {
	type = character_event
	title = saga.1.t
	desc = saga.1.desc
	theme = intrigue
	left_portrait = {
		character = root
		animation = personality_cynical
	}

	immediate = {
	}

	option = {
		name = saga.1.a
		add_prestige = minor_prestige_gain
	}

	option = {
		name = saga.1.b
		add_piety = minor_piety_gain
	}
}
